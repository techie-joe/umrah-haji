include _vars
include _mixins

block vars
  -
  //- layout vars

block entry
  //- entrypoint for variable setup & security check

doctype html
html#_html(lang=lang class=htmlClass)._nojs._scrollbar._a

  | <!-- umrah-haji:layout-r1 -->
 
  head
    meta(charset=charset)/
    
    block seo
      include _seo

    block meta
      include _meta

    block style
      +css(homeUrl+'assets/css/style.css?v='+version)
      +css(homeUrl+'assets/pure/grid.css?v='+version)

    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet"> 
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">

  body#_body(class=bodyClass)

    block content(class=contentClass)
      #_content

        block view
          ._view
            ._layout

              block header
                header#_header.g
                  .u.div-inline.main
                    <p><i class="fa fa-envelope mr-2"></i>info@example.com</p>
                    <p class="text-body px-3">|</p>
                    <p><i class="fa fa-phone-alt mr-2"></i>+012 345 6789</p>
                  .u._text-right
                    +_a("").faa.text-primary.px-3: +fa("facebook-f")
                    +_a("").faa.text-primary.px-3: +fa("twitter")
                    +_a("").faa.text-primary.px-3: +fa("linkedin-in")
                    +_a("").faa.text-primary.px-3: +fa("instagram")
                    +_a("").faa.text-primary.px-3: +fa("youtube")
                  //- 


              block nav
                //- include _nav
                //- nav: ul: +la

              block pre
                //- aside: pre._pre.
                //-

              block main
                main#_main
                  article
                    | hello world
  
              block footer
                //- footer#_footer
              
              block aside
                //- aside#_aside

    block widget
      //- aside#_widget

    block script
      +js(homeUrl+'assets/sjs/thm.js?v='+version)(defer)

    block last
      | <!-- IE needs 512+ bytes: https://techie-joe.github.io/library/html5/ie#ie-needs-512-bytes -->